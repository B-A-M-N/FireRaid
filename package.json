{
  "name": "fireraid",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "FireRaid — deterministic per-session admission defense lab against autonomous browser agents.",
  "scripts": {
    "dev": "wrangler dev --env dev",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run tests/unit",
    "test:integration": "node scripts/test-worker.mjs --suite test-integration --port 8799 -- vitest run --config vitest.integration.config.ts",
    "test:envelope": "node scripts/test-worker.mjs --suite test-envelope --port 8799 --wrangler-env production -- vitest run --config vitest.envelope.config.ts",
    "test:budget": "node scripts/budget-harness.mjs",
    "test:e2e": "playwright test",
    "test:a11y": "playwright test --config=playwright.a11y.config.ts",
    "lint": "eslint \"src/**/*.ts\" \"harness/**/*.ts\" \"tests/**/*.ts\" --max-warnings 0",
    "test:e2e:ci": "playwright install chromium firefox webkit && playwright test",
    "deploy": "wrangler deploy --env production",
    "deploy:lab": "wrangler deploy --env public-lab",
    "deploy:production": "wrangler deploy --env production",
    "experiment": "tsx harness/core/runner.ts",
    "analyze": "python3 harness/analysis/analyze.py"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260818.1",
    "@playwright/test": "^1.49.0",
    "@axe-core/playwright": "^4.10.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0",
    "tsx": "^4.19.0",
    "eslint": "^9.15.0",
    "@eslint/js": "^9.15.0",
    "typescript-eslint": "^8.15.0"
  }
}
