# FireRaid ATTACK-PLANE (harness) environment — DO NOT COMMIT REAL VALUES
# These credentials drive the agent adapters ONLY. Never reference them from
# src/ (the defense plane), and never put defense secrets in this file.

# LLM backend for raw-dom / ax-snapshot adapters
FIRERAID_LLM_BASE_URL=
FIRERAID_LLM_API_KEY=
# Model id the runner requests (FR-R7-011). OpenAI-compatible providers may
# alias this to a different served model; the runner records the served id
# returned by the completion endpoint separately in the run record.
FIRERAID_LLM_MODEL=

# Authenticated lab mode: lets the runner create lab runs + post outcomes
FIRERAID_LAB_API_SECRET=

# Set to any value to let the runner proceed with a dirty git tree (dev only)
FIRERAID_ALLOW_DIRTY=

# P1-POOL provider-pool posture (harness/.llm-pool.json, generated by
# scripts/transfer-crush-providers.mjs from the crush config):
#   (unset)  same-model rotation — pool candidates serve the SAME model id
#            the trial requested (provenance-safe fallback for 429/5xx)
#   substitute  DEGRADED MODE: final fallback may serve a DIFFERENT
#            advertised model (always recorded via llm_model_served +
#            llm_pool_provider — a run that substitutes is not a clean
#            measurement of the requested model)
#   off      disable the pool entirely (reproducibility runs)
FIRERAID_POOL=
